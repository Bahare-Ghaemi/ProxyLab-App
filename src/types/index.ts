export type TIcon = {
    size?: number
    color?: string
    className?: string
    width?: number
    height?: number
}

export type TTestimonial = {
    title: string,
    job: string,
    image: string,
    comment: string
}